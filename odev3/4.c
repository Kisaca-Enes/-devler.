#include <stdio.h>

int main() {
    int a = 1;
    
    if (a > 0) { printf("Pozitif"); }
    
    else if (a < 0) { printf("Negatif"); }
                
    else { printf("Nötür"); }
            
             
}
